{
  "name": "fleetrentals-web",
  "version": "1.0.0",
  "private": true,
  "description": "Fleet Rentals Flutter Web Application",
  "scripts": {
    "build": "flutter build web --release",
    "vercel-build": "bash vercel-build.sh",
    "deploy": "vercel --prod"
  }
}
